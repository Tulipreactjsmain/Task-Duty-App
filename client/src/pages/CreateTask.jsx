import { useStore } from "../config/store";

export default function CreateTask() {
  const { userData } = useStore();
//   const shouldRenderHello =
//     userData && location.pathname === `/${userData.username}/tasks/create`;
  return (
    // shouldRenderHello &
    // (
    //   <div>
    //     <h1>Create Task</h1>
    //     ';lkjhgc'
    //   </div>
    // )
    <>oijhjn</>
  );
}
